{
  "name": "course-repo-api",
  "version": "1.0.0",
  "description": "Self-contained course repository API",
  "main": "server.js",
  "scripts": {
    "start": "rm -f data.json && node server.js",
    "seed": "node seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
